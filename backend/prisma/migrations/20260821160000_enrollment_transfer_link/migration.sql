-- ------------------------------------------------------
-- رابطُ النقل — من الملاحظة إلى الكشف الآخر
--
-- الملاحظة تقول «مُنقَل من الفوج 1 · كشف الشهر 7»، ومن قرأها أراد أن
-- يفتحه. وكان عليه أن يعود إلى الشاشة ويعيد اختيار الطور والمستوى
-- والمادة والأستاذ والفوج ثمّ يبحث عن الكشف رقم 7 — خمسُ خطواتٍ
-- لمعلومةٍ بين يديه.
--
-- ونصُّ الملاحظة لا يُفكَّك ليُبنى منه رابط: تحليلُ الأسماء من جملةٍ
-- عربية هشٌّ يكسره أوّلُ فوجٍ اسمُه فيه فاصلة. فيُحفظ المقصدُ صريحاً.
--
-- ولا مفاتيحَ أجنبية عليهما عمداً: هما عنوانٌ لزرّ لا قيدٌ على البيانات.
-- وحذفُ كشفٍ أو إسنادٍ يجعل الزرّ بلا هدف — والواجهة تتحقّق من وجود
-- المقصد قبل أن تعرضه، فيختفي الزرّ ولا ينكسر النقل ولا الملاحظة.
-- ------------------------------------------------------

ALTER TABLE `StudentEnrollment`
  ADD COLUMN `transferPeerAssignmentId` VARCHAR(191) NULL,
  ADD COLUMN `transferSheetId` VARCHAR(191) NULL;
