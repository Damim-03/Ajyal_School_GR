-- ------------------------------------------------------
-- ملاحظةُ التسجيل — لا ملاحظةُ الطالب
--
-- عمودُ «ملاحظات» في الكشف كان يقرأ `Student.note`: حقلٌ واحدٌ للطالب
-- في المؤسسة كلِّها. فما يُكتب فيه يظهر في كشف الفرنسية وكشف العلوم
-- معاً، ويبقى بعد أن تنتهي مناسبتُه.
--
-- والنقلُ خبرٌ عن **تسجيلٍ بعينه**: «جاء من الفوج 1» تخصّ فوجَ
-- الإنجليزية وحده. فمحلُّها هنا، ولو كُتبت هناك لطمست ما كتبه الموظّف
-- ولأخبرت أساتذةً لا شأن لهم بها.
-- ------------------------------------------------------

ALTER TABLE `StudentEnrollment` ADD COLUMN `note` VARCHAR(191) NULL;
