-- ------------------------------------------------------
-- مكانُ ميلاد الطالب
--
-- تطلبه شهادةُ التمدرس صراحةً: «المولود(ة) في 12/03/2014 بـ: الجزائر».
-- وكان `address` وحده في المخطّط، وهو مسكنُه اليوم لا مسقطُ رأسه —
-- وبينهما فرقٌ في وثيقةٍ رسمية.
--
-- واختياريٌّ: الملفّاتُ القائمة لا تحمله، والشهادة تترك مكانه نقاطاً
-- تُملأ بالقلم حتّى يُكتب.
-- ------------------------------------------------------

ALTER TABLE `Student` ADD COLUMN `birthPlace` VARCHAR(191) NULL;
