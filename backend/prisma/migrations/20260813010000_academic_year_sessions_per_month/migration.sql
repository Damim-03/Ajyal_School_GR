-- ------------------------------------------------------
-- عدد الحصص المقرَّرة شهرياً — سياسة كل سنة على حدة
--
-- الافتراضي ثمانية لأنّه ما تعمل به المؤسسة اليوم، والسنوات
-- القائمة تأخذه فلا تتغيّر كشوفها. وتغييرُه لسنةٍ لاحقاً لا يمسّ
-- ما سبقها لأنّ القيمة مخزَّنة على السنة نفسها.
-- ------------------------------------------------------

ALTER TABLE `AcademicYear`
  ADD COLUMN `sessionsPerMonth` INTEGER NOT NULL DEFAULT 8;
