-- ------------------------------------------------------
-- صورة الطالب
--
-- يُخزَّن المسار لا الملف: الصور تُرفع إلى /uploads ويُحفظ
-- مسارها هنا، فلا تنتفخ قاعدة البيانات بملفّات ثنائية.
-- ------------------------------------------------------

ALTER TABLE `Student` ADD COLUMN `avatar` VARCHAR(191) NULL;
