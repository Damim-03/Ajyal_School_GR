-- ------------------------------------------------------
-- صورةُ المادة
--
-- المواد تُعرض مربّعاتٍ تُنقر — والعينُ تعرف الصورةَ قبل أن تقرأ الاسم.
-- وكان اللونُ وحده يفرّق بينها، وهو يفرّق ولا يُعرّف.
--
-- والمسارُ كما تُرجعه /api/uploads — كصورة الطالب وشعار المؤسسة، فلا
-- خدمةَ رفعٍ ثانية ولا مجلَّدٌ خاصّ.
-- ------------------------------------------------------

ALTER TABLE `Subject` ADD COLUMN `imagePath` VARCHAR(191) NULL;
