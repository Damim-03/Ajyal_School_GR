-- ------------------------------------------------------
-- وجهُ الورقة الممسوحة
--
-- الكشف التقديري يُطبع على وجهٍ واحد غالباً، وعلى وجهين حين يطول.
-- وكان المرفق مجرَّد ملفٍّ بلا صفة، فلا يُعرف أيُّ الصورتين الأمامية
-- إن مُسحتا معاً — ولا يُعرف أنّ الخلفية ناقصة إن نُسيت.
--
-- والحقل **اختياري**: ورقةٌ من وجهٍ واحد لا خلفيّة لها، والأوراق
-- الملحقة قبل اليوم لا وجهَ لها فتبقى كما هي.
-- ------------------------------------------------------

ALTER TABLE `SettlementDocument`
  ADD COLUMN `side` ENUM('FRONT', 'BACK') NULL;
