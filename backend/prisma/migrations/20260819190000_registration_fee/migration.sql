-- ------------------------------------------------------
-- حقوق التسجيل
--
-- مبلغٌ يُدفع مرّةً واحدة حين يُسجَّل الطالب في المؤسسة — غيرُ الحقّ
-- الشهري الذي تحمله الفاتورة. وكان يُقبض في الشبّاك ولا أثر له في
-- النظام، فلا يُعرف بعد شهرٍ أدفعه هذا الطالب أم لا.
--
-- وثلاثةُ حقولٍ لا حقلٌ واحد: الحالة وحدها تقول «دُفع» ولا تقول كم
-- ولا متى — والوصلُ يحتاجهما.
-- ------------------------------------------------------

ALTER TABLE `Student`
  ADD COLUMN `registrationFeePaid`   BOOLEAN NOT NULL DEFAULT 0,
  ADD COLUMN `registrationFeeAmount` DECIMAL(10, 2) NULL,
  ADD COLUMN `registrationFeePaidAt` DATETIME(3) NULL;
