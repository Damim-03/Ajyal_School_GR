node_modules
# Keep environment variables out of version control
.env

/src/generated/prisma

# Uploaded user files — not app config, shouldn't live in git
/uploads/*
!/uploads/.gitkeep

# نسخُ الصيانة — بيانات المؤسسة لا شيفرة
backups/
