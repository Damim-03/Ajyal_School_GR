import { defineConfig } from "vitest/config";

// ======================================================
// اختباراتُ طبقة التقارير.
//
// `node` لا `jsdom`: هذه دوالُّ حسابٍ نقيّة على الخادم، ولا شيء
// فيها يلمس DOM. وتحميلُ بيئة متصفّحٍ لها إبطاءٌ بلا مقابل.
// ======================================================

export default defineConfig({
  test: {
    environment: "node",
    include: ["src/**/*.test.ts"],
    /*
     * الاختباراتُ هنا لا تلمس قاعدةَ البيانات إطلاقاً — تأخذ
     * أعداداً مجمّعةً وتتحقّق من الحساب. ولذلك تعمل على استضافةٍ
     * حصّتُها من الاتصالات محدودة، وتعمل قبل أن توجد بياناتٌ أصلاً.
     */
    globals: false,
  },
});
