import { defineConfig } from "vitest/config";

// ======================================================
// اختباراتُ الواجهة.
//
// `node` لا `jsdom`: المُختبَرُ هنا حسابُ المقاييس — دوالُّ نقيّة
// لا تلمس DOM. وتحميلُ بيئة متصفّحٍ لها إبطاءٌ بلا مقابل، ويُضيف
// تبعيةً ثقيلة لأجل اختباراتٍ لا تحتاجها.
//
// وحين تُختبر المكوّناتُ لاحقاً تُضاف بيئتُها حينئذٍ.
// ======================================================

export default defineConfig({
  test: {
    environment: "node",
    include: ["src/**/*.test.ts"],
    globals: false,
  },
});
