/**
 * الشريط العلوي — الأرقام وحدها.
 *
 * مأخوذةٌ من مخطّط التصميم حرفياً (اللوحة اللونية، وقياسات المناطق،
 * ومنحنى الحركة ومددها)، ومجموعةٌ هنا لا مبعثرةٌ في `className`: تعديلُ
 * قطر الأيقونة أو شدّة التوهّج يقع في سطرٍ واحد بدل تسعة مواضع متشابهة.
 */

/* ============================================================
 * 1 — اللوحة اللونية
 * ============================================================ */

/**
 * الستّة التي تحدّدها المواصفة. وثلاثةٌ منها للأسطح، وواحدٌ للنصّ
 * النشط، واثنان لِلَمعة الهويّة.
 *
 * وهي مكتوبةٌ هنا لا في `index.css` لأنّها تخصّ هذا السطح وحده — ولو
 * صارت متغيّرات CSS عامّة لأوهمت أنّها لغةُ التطبيق كلِّه، وليست كذلك.
 */
export const palette = {
  /** أعمق قاع — خلف الشريط عند أطرافه */
  abyss: "#0D1117",
  /** سطحُ الأيقونة الخاملة */
  surface: "#161C28",
  /** حافّتُها */
  edge: "#1F2735",
  /** النشط: خلفيةٌ بيضاء وأيقونةٌ داكنة */
  active: "#FFFFFF",
  /** لمعةُ الهويّة — البنفسجي الفاتح */
  accentSoft: "#A5B4FC",
  /** الأزرق القويّ — المؤشّر السفلي تحت التبويب النشط */
  accent: "#4C9AFF",
} as const;

/* ============================================================
 * 2 — القياسات
 * ============================================================ */

export const geometry = {
  /** قطرُ الأيقونة الدائرية. */
  icon: 48,
  /** الفجوةُ بينها — أضيقُ من نصف قطرها فتُقرأ صفّاً واحداً لا أفراداً. */
  gap: 12,
  /** قطرُ صورة الملف الشخصي (منطقةُ 120px في المخطّط). */
  avatar: 44,
  /** ارتفاعُ الشريط كلِّه. */
  height: 72,
} as const;

/* ============================================================
 * 3 — الحركة
 * ============================================================ */

/**
 * منحنى المخطّط حرفياً: `cubic-bezier(0.23, 1, 0.32, 1)`.
 *
 * وهو قريبٌ جداً من `easing.enter` في نظام الحركة (‏0.22,1,0.36,1) —
 * والفارقُ في الرقم الأخير وحده. أُبقي رقمُ المخطّط عمداً: هذا السطح
 * مواصفةٌ مرسومة، ومطابقتُه حرفياً تجعل مراجعتَه ممكنة. ولو تباعدا
 * لاحقاً وجب التوحيد.
 */
export const ease = [0.23, 1, 0.32, 1] as const;

export const cssEase = `cubic-bezier(${ease.join(",")})`;

/**
 * إبرازُ الأيقونة عند التثبيت عليها.
 *
 * ثلاثُ محطّاتٍ من المخطّط: 0.96 ← 1.08 ← 1. والوسطى تجاوزٌ مقصود
 * (‏8%) — وهو **الاستثناء الوحيد** في هذا السطح: بقيّةُ النظام تمنع
 * الارتداد، لكنّ هذه ليست سفراً بل **استجابةً للمسّ**، وهي الفئةُ
 * نفسُها التي يُسمح فيها بالرِجعة في `spring.press`.
 *
 * ولا تقع إلّا عند الدخول إلى الأيقونة؛ والخروجُ يعود بلا لقطات.
 */
export const highlight = {
  scale: [0.96, 1.08, 1],
  times: [0, 0.55, 1],
  /** 220ms — وسطُ النطاق الذي تحدّده المواصفة (200–250). */
  duration: 0.22,
} as const;

/** التوهّجُ حول النشط — ناعمٌ لا نيون. */
export const glow = {
  idle: "none",
  active: "0 0 0 1px rgba(255,255,255,0.18), 0 0 22px rgba(255,255,255,0.28)",
} as const;

/** ظهورُ اسم الأيقونة تحتها. */
export const label = {
  duration: 0.16,
  y: -4,
} as const;

/**
 * التبويبات (الرئيسية / الأقسام).
 * التكبيرُ 1.1 من المخطّط، وهو كبيرٌ على نصّ — فقُصر على المتاح للنقر.
 */
export const tab = {
  hoverScale: 1.06,
  /** المؤشّر السفلي: سُمكُه وعرضُه ومنحنى انزلاقه بين التبويبين. */
  indicatorHeight: 2,
  duration: 0.24,
} as const;

/** نسخةٌ مبسّطة عند تفضيل «تقليل الحركة». */
export const reduced = {
  transition: { duration: 0.12, ease: "easeOut" },
} as const;
