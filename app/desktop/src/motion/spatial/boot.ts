/**
 * تسلسل استيقاظ العالم — مالكٌ واحد لتوقيت الإقلاع كلّه.
 *
 * ما كان: ثلاثة أرقام (‏300، 670، 1170) داخل تعليق في الرئيسية، تفتح
 * الخلفيةَ ثم كلَّ شيء آخر دفعةً واحدة. عملياً كانت الواجهة **تُعرَض** لا
 * تُجمَّع: الشريط يظهر ككتلة واحدة، وحلقة التركيز تُرسم جاهزة، والبطل
 * يهبط بلا أصل.
 *
 * ما صار: ثماني مراحل، كلٌّ منها تبدأ **قبل أن تنتهي سابقتها**. التراكب
 * هو ما يفصل «العالم يستيقظ» عن «قائمة عناصر تظهر بالدور» — التتابع
 * الحادّ يُقرأ آلياً مهما نعُمت كل حركة على حدة.
 *
 * ترتيب الإضاءة مقصود وهو عمود الفصل: ضوء محيط ← وضوح الخلفية ← أسطح
 * البلاطات ← إضاءة المادّة عند وصول الطاقة ← تعريف الحافّة آخراً. لو
 * ظهرت الحافّة مع السطح لبدت البلاطة مرسومةً كاملةً، لا مُضاءة تدريجياً.
 *
 * الزمن بالمللي ثانية من لحظة تركيب الرئيسية.
 */
export const boot = {
  /**
   * ① العالم يستيقظ: الظلام ينحسر، ويظهر الضوء المحيط والجوّ.
   * يبدأ عند 60ms لا عند الصفر — لحظةُ سكونٍ قبل أول ضوء تجعل ما بعدها
   * يُقرأ كاستيقاظ لا كتحميل انتهى.
   */
  world: { at: 60, duration: 720 },

  /** ② البيئة تستقرّ: خلفية القسم تبلغ وضوحها الكامل. */
  stabilize: { at: 320 },

  /** ③ هويّة النظام: الشريط العلوي — الساعة، المستخدم، المؤشّرات. */
  identity: { at: 560, duration: 420 },

  /**
   * ④ تجميع الشريط: كل بلاطة تدخل على حدة، والتتابع **من المركَّزة نحو
   * الأطراف** لا من اليمين إلى اليسار. الصفّ بنيةٌ لها مركز، لا قائمةٌ
   * تُقرأ بالترتيب — والمركز هو الوجهة التي سيقف عندها المستخدم.
   */
  assembly: {
    at: 700,
    duration: 320,
    stagger: 40,
    /**
     * ارتفاع الصفّ **ككلّ** أثناء تجميعه.
     *
     * كانت كل بلاطة ترتفع وحدها (‏14px) والصفّ الحاوي ساكن — فيُقرأ تسع
     * قِطعٍ تُركَّب متجاورةً، لا بنيةً واحدة تُبنى. الآن يرتفع الصفّ 10px
     * كجسمٍ واحد بينما تتعاقب أعضاؤه داخله: التتابع يخصّ الأعضاء،
     * والحركة الجامعة تخصّ الصفّ. اجتماعهما هو ما يجعله «شيئاً واحداً».
     */
    rise: 10,
  },

  /**
   * ⑤ وصول الطاقة: تسافر من خارج الصفّ إلى البلاطة المحدَّدة.
   * لا تُشعَل هناك — تصل. وهذا ما يجعل الاختيار يبدو حدثاً وقع، لا حالةً
   * كانت قائمة أصلاً.
   */
  focus: {
    at: 1020,
    /**
     * مدّة وصول **اسم القسم** تحت الصفّ.
     *
     * كان الاسم مكتوباً على الشاشة منذ الإطار الأول: قِسته فوجدته عند
     * شفافية 1 بينما الترويسة والبلاطات كلّها عند الصفر — أي نصٌّ يسبح في
     * سوادٍ مطلق قبل أن يوجد شيء يسمّيه. وهو نقض المرحلة ⓪ حرفياً.
     *
     * الاسم يخصّ البلاطة التي وصلتها الطاقة، فلا يجوز أن يسبقها.
     */
    duration: 340,
  },

  /** الحافّة آخر ما يُعرَّف — حلقة التركيز بعد أن تستقرّ الطاقة. */
  edge: { at: 1200 },

  /** ⑥ ولادة البطل، ومنها ينطلق الكشف التدريجي بطبقاته السبع. */
  hero: { at: 1240 },

  /**
   * المسافة التي تقطعها الطاقة داخلةً، بالبلاطات.
   * 2.6 لا 1: الرحلة يجب أن تُرى. ودون 2 تبدو ارتجافةً لا سفراً.
   */
  entryTravel: 2.6,

  /**
   * تقريب الكاميرا الابتدائي — المشهد يبدأ أقرب قليلاً ثم ينسحب إلى
   * موضعه. 1.05 حدٌّ أعلى: ما فوقه يصير حركة كاميرا واعية، والمطلوب
   * حضورٌ لا استعراض.
   */
  dolly: 1.06,
  dollyMs: 1100,
} as const;

/** مراحل الإقلاع مرتَّبة — الرئيسية تتقدّم فيها بمؤقّت واحد. */
export const BOOT_STAGES = [
  boot.world.at,
  boot.stabilize.at,
  boot.identity.at,
  boot.assembly.at,
  boot.focus.at,
  boot.edge.at,
  boot.hero.at,
] as const;

/** أسماء المراحل — تُقرأ في الشيفرة بدل أرقام. */
export const STAGE = {
  dark: 0,
  world: 1,
  stabilize: 2,
  identity: 3,
  assembly: 4,
  focus: 5,
  edge: 6,
  hero: 7,
} as const;
