/**
 * رموز حركة التنقّل المكاني — المصدر الوحيد لكل توقيت ونابض في الشريط.
 * لا تكتب `transition: 237ms` في أي مكان آخر؛ عدّل هنا فقط.
 *
 * الأرقام الهندسية مقيسة إطاراً بإطار من تسجيل مرجعي (30fps، مطابقة قوالب
 * متعدّدة المقاييس): نسبة المركَّز 1.563، الفجوة 0.081 من البلاطة،
 * وتنقّل التركيز يستقرّ خلال ~133–200ms.
 */

import { spring } from "../system";

/**
 * نوابض: انطلاق حاسم ← انزلاق سريع ← استقرار مغناطيسي ناعم.
 *
 * نسبة التخميد ζ = damping ÷ (2·√(stiffness·mass)) تحكم الشخصية:
 *   ζ = 1     تخميد حرج — وصول بلا تجاوز لكن نهايته «تتوقّف» فجأة قليلاً
 *   ζ ≈ 0.93  تجاوز ~1% غير مرئي واعياً + ذيل استقرار ناعم ← المطلوب
 *   ζ < 0.85  ارتداد مرئي ← مرفوض
 * كل القيم أدناه مضبوطة عند ζ ≈ 0.92–0.95: يُقطع أول 70% من المسافة
 * بسرعة ثم يلين الربع الأخير، مع «استقرار دقيق» طبيعي بلا حيلة إضافية.
 */
/**
 * النوابض تُشتقّ من المصدر الموحّد في motion/system.ts، فتتشارك الرئيسية
 * والنوافذ والإشعارات شخصية فيزيائية واحدة بدل ثلاث شخصيات متقاربة عشوائياً.
 * (`restDelta` الصغير هناك يجعل النابض يواصل التقارب حتى تقترب سرعته من
 *  الصفر فعلاً، فلا توجد لحظة «انتهى الانتقال» محسوسة.)
 */
export const springs = {
  navigation: spring.navigation,
  tile: spring.tile,
  focus: spring.focus,
  icon: spring.icon,
  press: spring.press,
} as const;

/** مدد الطبقات غير النابضة (ثوانٍ — وحدة motion). */
export const durations = {
  labelOut: 0.16,
  labelIn: 0.22,
  secondary: 0.24,
  background: 0.55, // الخلفية أبطأ عمداً: تدرّج عمق بصري
} as const;

/**
 * الكشف التدريجي — سبع طبقات، ترتيبها **دلالي** لا تزييني:
 *
 *   ① الهويّة        ما هذا المكان؟          (السطر التعريفي والعنوان)
 *   ② الفعل          ماذا أستطيع أن أفعل؟    (زرّ الدخول)
 *   ③ التفاصيل       ما الذي يعنيه؟           (الوصف، ينفتح من العنوان)
 *   ④ الحالة         هل هناك ما يستدعي الآن؟  (سطر واحد بلون دلالي)
 *   ⑤ الأرقام        كم؟                      (الإحصاءات)
 *   ⑥ النشاط         ماذا حدث للتوّ؟          (آخر العمليات)
 *   ⑦ الفروع         أين أذهب داخله؟          (بطاقات الإجراءات)
 *
 * كانت الحالة والأرقام والنشاط تظهر ككتلة واحدة بتأخير واحد — أي أنّ
 * «هناك 921 منتجاً منتهي الصلاحية» يصل إلى العين في اللحظة نفسها التي
 * تصل فيها تفاصيلُ لا تستدعي تصرّفاً. الطبقات تجعل الأهمّ يسبق.
 *
 * الفواصل صغيرة (‏40–60ms) وكل طبقة تبدأ قبل انتهاء سابقتها بكثير: الشاشة
 * بيئة واحدة تستجيب، لا مكوّنات تُبدّل حالتها بالدور.
 *
 * وآخر بطاقة تنتهي عند ~515ms، قبل استقرار الخلفية (520ms) — **البيئة
 * تُغلق المشهد**. هذه علاقةٌ يجب أن تبقى محفوظة عند أيّ ضبط: إن تجاوز
 * المحتوى الخلفيةَ انقلب الترتيب صامتاً وسبقت البيئةُ المحتوى إلى السكون.
 */
export const choreography = {
  /*
   * **الجدول مضغوط إلى نطاق المرجع.**
   *
   * كان الكشف يمتدّ إلى 700ms وتُغلقه الخلفية عند 780ms — أي أكثر من
   * ضعف ما يحدّده المرجع («الانتقالات الأساسية 180–300ms، والسقف 500ms
   * تفادياً لنفاد صبر المستخدم»). صار يستقرّ عند ~515ms.
   *
   * والنِّسَب محفوظة كما في مخطّط المرجع (‏Focus→Hero→Context): يبدأ
   * العنوان عند 120ms — أي في الثلث الأخير من سفر التركيز (‏184ms) لا
   * بعده — فيتراكب الطوران قليلاً كما تنصّ الوثيقة، ثم يتتابع الباقي
   * بفواصل 30–40ms.
   *
   * ولم يعد أيّ عنصر يستغرق أطول من 220ms وحده.
   */
  title: { delay: 0.12, duration: 0.2, y: 8 },
  /*
   * الوصف **قبل** الفعل — تبديلٌ متعمَّد للترتيب.
   *
   * كان الزرّ يسبق الوصف: أي أنّ الواجهة تعرض
   * «التزِم» قبل أن تُتمّ جملة «هذا ما ستلتزم به». والقاعدة المعلنة أنّ
   * الزرّ **التزام لا فضول**، فلا يجوز أن يصل قبل الفهم.
   *
   * والترتيب محفوظ بعد ضغط الجدول كلّه: الوصف يبقى قبل الفعل مهما
   * تغيّرت المدد.
   */
  desc: { delay: 0.16, duration: 0.2, y: 8 },
  action: { delay: 0.2, duration: 0.18, y: 6 },
  status: { delay: 0.23, duration: 0.18, y: 5 },
  stats: { delay: 0.25, duration: 0.18, y: 6 },
  activity: { delay: 0.27, duration: 0.18, y: 5 },
  cards: { delay: 0.29, duration: 0.2, y: 8, stagger: 0.025 },
  /** الشريط العلوي يتراجع بصرياً أثناء التنقّل فلا يزاحم المحتوى. */
  chromeIdleOpacity: 0.62,
} as const;

/**
 * مراحل انكشاف السياق — ثماني مراحل متراكبة، من لحظة التأكيد.
 *
 * الفرق عن `choreography`: تلك تصف استجابة الشاشة **لتبدّل التركيز**،
 * وهذه تصف **انفتاح الرئيسية على نفسها**. حدثان مختلفان لا يصحّ أن
 * يتشاركا جدولاً واحداً.
 *
 * القاعدة نفسها: كل مرحلة تبدأ قبل انتهاء سابقتها، ولا شيء يقف مع شيء.
 * والمدد متفاوتة عمداً — التساوي يجعل النهايات تتزامن فتُقرأ آلية.
 */
/**
 * **الصمت البصري** — إطاران يستقرّ فيهما النظر.
 *
 * مفهومٌ من المرجع لم يكن عندنا: «فجوة قصيرة (‏16–33ms) تُدرَج عند لحظات
 * مفتاحية — مباشرةً بعد حركة كبيرة — كي تستقرّ العين. ليست تأخيراً
 * يُدرِكه المستخدم، بل فاصلٌ مقصود للتجميع الإدراكي».
 *
 * 32ms = إطاران عند 60fps. تُستعمل بين **المقاومة** وبين بدء امتداد
 * الصفحة: يُقاوم البطل، ثم صمت، ثم ينفتح العالم. بلا هذا الفاصل يذوب
 * الحدثان في حركةٍ واحدة ويضيع أثر المقاومة.
 */
export const silence = 0.032;

export const expansion = {
  /**
   * ⓪ **المقاومة** — اللحظة التي كانت مفقودة كلّياً.
   *
   * كان التأكيد يفتح السياق في الإطار نفسه الذي وصلت فيه الضغطة: إدخالٌ
   * ثمّ نتيجة، بلا شيء بينهما. وهذا بالضبط ما يجعل الواجهة تُقرأ رقميّة لا
   * ميكانيكية — الأجسام الحقيقية تُقاوم قبل أن تتحرّك.
   *
   * فيُدفع البطل إلى **الداخل** قليلاً أوّلاً (‏y موجب، وانضغاط 0.65%) ثم
   * يُطلَق. المدّة 80ms — داخل نطاق «التأخيرات الدقيقة» الذي يحدّده
   * المرجع (‏50–100ms)، وكانت 120ms أي فوقه. والصفحة لا تبدأ امتدادها إلا
   * بعد انقضائها وبعد صمتٍ بصريّ، فيُقرأ الترتيب سبباً ونتيجة.
   */
  brace: { duration: 0.08, scale: 0.9935, y: 3 },
  /**
   * ① تأكيد البطل — يتحرّك مع الصفحة لا يقف متفرّجاً.
   *
   * `settle` أطول من `duration` عمداً: الانضغاط ينتهي قبل الارتفاع، فلا
   * يستقرّ محورا البطل في اللحظة نفسها (§15 — لا شيء يقف مع شيء).
   */
  hero: { delay: 0, duration: 0.28, settle: 0.34 },
  /**
   * مادّة البطل. الظلّ يعمق مع اتّساع الصفحة تحته: جسمٌ ارتفع عن سطحٍ
   * صار أبعد. الفارق محسوب ليُدرَك عمقاً ولا يُرى تغيّراً في اللون.
   */
  heroMaterial: {
    rest: "drop-shadow(0 4px 28px rgba(0,0,0,0.80))",
    deep: "drop-shadow(0 9px 40px rgba(0,0,0,0.92))",
  },
  /**
   * انحسار الصفّ (§10). الصفّ **يشارك** ولا يتفرّج: يرتفع 6px مع البطل،
   * وتخفت البلاطات غير المركَّزة وحدها.
   *
   * المركَّزة تبقى عند 1 بلا استثناء — هي الأصل الذي انبثق منه كلّ ما
   * انفتح (§5)، وخفتُها كان سيقطع النسب في اللحظة التي يجب أن يتأكّد فيها.
   */
  row: { dim: 0.62, y: -6, duration: 0.34 },
  /**
   * ⑨ البيئة تُغلق المشهد — آخر ما يستقرّ.
   *
   * 520ms: بعد آخر طبقة محتوى (‏الثانوي ينتهي عند 480ms) وقبل أن يطول
   * الأمر فيُقرأ تباطؤاً. كانت 820ms — أي أنّ العالم يواصل تراجعه بعد أن
   * استقرّ كلّ ما فوقه بثلث ثانية.
   */
  world: { duration: 0.52, light: 0.05 },
  /** ② الصفحة تمتدّ — ارتفاع السياق من صفر. */
  open: { delay: silence, duration: 0.3 },
  /** ③ الإجراء الأساسي يستقرّ في وزنه الجديد. */
  action: { delay: 0.06, duration: 0.22 },
  /** ④ الحالة والأرقام. */
  status: { delay: 0.1, duration: 0.2, y: 5 },
  stats: { delay: 0.13, duration: 0.2, y: 6 },
  /** ⑤ النشاط الأخير. */
  activity: { delay: 0.16, duration: 0.2, y: 5 },
  /**
   * ⑥⑦ الفروع — **مجموعات لا دفعة واحدة**.
   * كل مجموعة صفّ من بطاقتين، وبينهما فاصل، وداخل المجموعة تتابع أدقّ.
   */
  group: { delay: 0.19, duration: 0.22, y: 8, between: 0.05, within: 0.03 },
  /** ⑧ الثانوي — سطر الإرشاد، آخر ما يصل. */
  secondary: { delay: 0.28, duration: 0.2, y: 4 },
  /** حجم المجموعة الواحدة. */
  groupSize: 2,
} as const;

/** تأخيرات صغيرة جداً — طبقات متراكبة لا تتابع مرئي. */
export const delays = {
  tile: 0.015,
  focus: 0.02,
  labelOut: 0.06,
  labelIn: 0.12,
  secondary: 0.06,
} as const;

/**
 * مقاس البلاطة المضغوطة بالبكسل.
 *
 * نفس صيغة `tileCss` تماماً (‏clamp 48 / 5.51vw / 106) لكن كعدد.
 * الحاجة إليه: الحجم صار مقوداً بالطاقة لا مُبدَّلاً بين قيمتين، وقيم
 * CSS النصّية (calc/clamp) لا تُستوفى رقمياً.
 *
 * القيمتان مشتقّتان من الثوابت نفسها أدناه، فلا تنحرف إحداهما عن الأخرى.
 */
export function compactPx(viewportWidth: number): number {
  return Math.min(TILE_MAX, Math.max(TILE_MIN, viewportWidth * TILE_VW));
}

const TILE_MIN = 48;
const TILE_MAX = 106;
const TILE_VW = 0.0551;

/** الهندسة — نِسَب لا أرقام ثابتة، فتتناسب مع أي مقاس بلاطة. */
export const geometry = {
  /** البلاطة نسبة من عرض النافذة (المرجع: 5.54%). */
  tileCss: "clamp(48px, 5.51vw, 106px)",
  /**
   * المركَّز ÷ المضغوط.
   *
   * القياس من المرجع أعطى 1.563، فخُفِّض إلى 1.42 عن قصد: علاقة «مضغوط
   * ← متمدّد» لا «ضئيل ← ضخم».
   *
   * **وخُفِّض مرّةً إلى 1.26 ثم أُعيد.** السبب أنّ وثيقة المرجع تنصّ على
   * «‏1.03–1.05، والزوم الكبير مثل 1.5× يُتجنَّب» — فطُبّق الرقم. ثمّ
   * تبيّن أنّه يناقض دليلين أقوى منه:
   *
   *   • **لقطة واجهة الكونسول نفسها**: البلاطة المركَّزة نحو 90px
   *     والمجاورة نحو 60px — أي ‏1.5× تقريباً، لا 1.05.
   *   • **قياس هذا المشروع** إطاراً بإطار: 1.563.
   *
   * والتفسير أنّ الوثيقة تعمّم عن تكبير عناصر الواجهة (بطاقات، أزرار،
   * طبقات) حيث 1.03–1.05 صحيح، أمّا **صفّ الرئيسية** فحالة خاصّة يكتسب
   * فيها التسلسل الهرمي من الحجم — ولذلك يكتفي المرجع بتعتيم الجيران إلى
   * 0.8 لا أكثر. القناتان متلازمتان: حجمٌ قويّ وتعتيمٌ خفيف.
   *
   * الدرس: رقمٌ في وثيقة تحليل لا يعلو على قياسٍ من المصدر.
   */
  selectedRatio: 1.42,
  /**
   * الفجوة بين المضغوطتين.
   *
   * 0.18 مقيسة مباشرةً من ملف سطوع المرجع: الفجوة تظهر كقمّة واضحة عرضها
   * 10.4–11.7px، وتباعد المراكز 72px، فالبلاطة 61px ⇒ 11/61 = 0.18.
   *
   * القيمة السابقة (0.081) كانت خطأ قياس: اشتُقّ عرض البلاطة من مطابقة
   * قوالب على فنّ الغلاف لا من حوافّ البلاطة نفسها، فبولغ في العرض
   * وقُلّلت الفجوة إلى أقلّ من نصفها.
   */
  gapRatio: 0.18,
  /** الرمز داخل البلاطة المضغوطة. */
  iconRatio: 0.523,
  /** الرمز يكبر أقلّ بكثير من الحاوية — سرّ إحساس «تمدّد البلاطة». */
  iconSelectedScale: 1.06,
  radiusRatio: 0.182,
  ringOffsetRatio: 0.057,
  /** حدّ رفيع: الإطار مؤشّر موضع لا عنصر زخرفي. */
  ringWidth: 1.5,
  ringColor: "rgba(255,255,255,0.86)",
  /**
   * موضع استقرار البلاطة المركَّزة، نسبةً من حافة بداية العرض.
   * مقيس من المرجع: مركز البلاطة المركَّزة ثابت عند 136px من أصل 730
   * (‏18.6%) — قريب من الحافة عمداً، فيبقى الفضاء الأكبر للمحتوى.
   * كلّما صغرت القيمة انزلق الصفّ أكثر؛ عند 1 يتوقّف الانزلاق تماماً.
   * خُفِّضت إلى 0.14 لتثبيت التركيز على مدى أوسع من البلاطات: كل بلاطة
   * من الثانية فصاعداً تستقرّ عند النقطة نفسها، والشريط هو ما يتحرّك.
   */
  focusAnchor: 0.14,
} as const;

/*
 * تدرّج القرب من التركيز انتقلت ملكيّته إلى نموذج الطاقة في
 * motion/energy.ts.
 *
 * كان هنا جدولٌ بأربعة صفوف وأربع خصائص — ستّة عشر رقماً مستقلّاً. مشكلته
 * أنّ كل خاصّية قرار منفصل: تعدّل الشفافية فينجرف السطوع عنها، ولا يبقى
 * أحد قادراً على الإجابة عن السؤال الوحيد المهمّ — «كم يبدو هذا العنصر
 * مركَّزاً عليه؟».
 *
 * الآن قيمة واحدة 0 → 1 تشتقّ الأربعة. الشكل لم يتغيّر (أقصى انحراف عن
 * الجدول المقيس 0.03)، لكن أصبح للتدرّج **مالك واحد** (§65) تتوصّل به
 * فصولُ المادة والإضاءة لاحقاً فتبقى متسقة تلقائياً.
 */

/**
 * استجابة الجيران **المكانية** — آخر ما كان ينقص في «الصفّ جسمٌ واحد».
 *
 * كان الجيران يعترفون بانتقال الانتباه بالضوء والظلّ والشفافية وحدها،
 * أمّا تباعدهم فثابت: الفجوة رقمٌ لا يتغيّر، وما يزيح البلاطات هو تمدّد
 * المركَّزة في التخطيط لا غير. فالصفّ يستجيب **مادّياً** ولا يستجيب
 * **هندسياً** — وهذا ما يُبقي فيه أثراً من «عناصر متجاورة».
 *
 * الآن تُدفع كل بلاطة بعيداً عن موضع الطاقة بمقدارٍ ضئيل: تبلغ ذروتها عند
 * الجار المباشر وتنحسر إلى الصفر عند البلاطة الثالثة، والمركَّزة نفسها لا
 * تُدفَع (هي مصدر الدفع لا موضوعه). فيُقرأ الصفّ جسماً مرناً تتباعد
 * أجزاؤه قليلاً حيث يحلّ الثقل.
 *
 * ولماذا `transform` لا `gap`: تحريك الفجوة تغييرُ تخطيط، و`ResizeObserver`
 * في `SpatialNavRow` يراقب الصفّ فيُعيد حساب المرساة في **كل إطار** — أي
 * إعادة عرض React ستّين مرّة في الثانية أثناء كل تنقّل. الإزاحة هنا على
 * المعالج الرسومي وحده، ولا تمسّ `offsetLeft` الذي تُقاس منه المرساة.
 */
export const neighbor = {
  /** أقصى إزاحة (px) — عند الجار المباشر. */
  push: 2.5,
  /** أبعد من هذه المسافة لا دفع. */
  reach: 3,
} as const;

export const depth = {
  hoverScale: 1.03,
  pressScale: 0.975,
  /** الرمز يرتفع قليلاً داخل البلاطة المتمدّدة — فصلٌ عن حركة الحاوية. */
  iconLift: -1.5,
} as const;

/**
 * الخلفية: طبقة سينمائية تتخلّف عن المقدّمة عمداً.
 * تبدأ بعد 60ms من التنقّل وتنتهي بعد استقرار البلاطة بكثير.
 */
export const background = {
  /**
   * تبدأ بعد 60ms من التنقّل وتنتهي عند 520ms — آخر الطبقات جميعاً.
   *
   * القيمة مربوطة بنهاية آخر بطاقة (‏~515ms) لا مختارة على حدة: كلّما
   * تغيّر جدول الكشف وجب أن تتبعه هذه. وقع الخطأ مرّتين قبلاً — انتهت
   * الخلفية قبل البطاقات فسبقت المحتوى إلى السكون.
   *
   * وقد نزلت من 0.72 مع ضغط الجدول كلّه إلى نطاق المرجع.
   */
  duration: 0.46,
  delayIn: 0.06,
  /** الخارجة تتمدّد قليلاً وهي تختفي، والداخلة تهبط من تكبير طفيف. */
  exitScale: 1.015,
  /** إزاحة أفقية دقيقة باتجاه التنقّل (px) — بضعة بكسلات لا أكثر. */
  shift: 5,
} as const;

/** نسخة مبسّطة تُستعمل عند تفضيل «تقليل الحركة». */
export const reduced = {
  transition: { duration: 0.12, ease: "easeOut" },
} as const;
