/**
 * طبقات العمق البصري (§32/§33).
 *
 * المشكلة التي يحلّها: كانت قيم z مبعثرة كأرقام حرفية في خمسة عشر موضعاً —
 * `z-10`، `z-[100]`، `z-[999]`، `zIndex: 88`، `zIndex: 5` — ولا أحد يعرف
 * لماذا الجولة الإرشادية عند 88 والإشعار عند 120. أي طبقة جديدة كانت
 * تُضاف بالتخمين ثم تُصحَّح بعد أن تختفي خلف شيء ما.
 *
 * هنا **ترتيب دلالي** واحد: الطبقة تُسمّى بوظيفتها لا برقمها. الأرقام
 * مطابقة تماماً لما كان مستعملاً، فلم يتغيّر ترتيب أي عنصر — التوحيد
 * معماري لا بصري.
 *
 * قاعدة: لا تكتب رقم z في مكوّن. أضِف اسماً هنا إن احتجت مستوى جديداً.
 */
export const LAYER = {
  /** خلف كل شيء — هالة البلاطة المركَّزة تحت سطحها. */
  beneath: -1,
  /**
   * جوّ القسم خلف محتوى الشاشة كلّه.
   *
   * نفس الرقم و**غير** `beneath` قصداً: ذاك يعمل داخل سياق تكديس بلاطة
   * واحدة، وهذا على جذر المستند. توحيدهما في اسم واحد كان سيُخفي أنّ
   * تغيير أحدهما لا علاقة له بالآخر. الصفحات هنا غير مموضعة، فطبقةٌ
   * مثبَّتة بترتيب موجب ترتسم فوق نصّها لا خلفه — ومن هنا السالب.
   */
  world: -1,
  /** البيئة: الخلفية، الضباب، الجسيمات. */
  environment: 0,
  /** بلاطة غير مركَّزة في الصفّ. */
  navItem: 1,
  /** البلاطة المركَّزة — ترتفع فوق جيرانها فلا يقصّها أحد. */
  navItemFocused: 5,
  /** محتوى الشاشة فوق البيئة. */
  content: 10,
  /** أشرطة لاصقة وألواح جانبية داخل الصفحة. */
  sticky: 50,
  /**
   * قائمة الزرّ الأيمن.
   *
   * فوق الألواح واللاصق (تُفتح عليها فيجب أن تُرى)، ودون الحوار: الحوار
   * يأخذ التركيز كلّه، وقائمةٌ تطفو فوقه تعني أنّها بقيت مفتوحة من شاشةٍ
   * لم تعد ظاهرة.
   */
  contextMenu: 55,
  /** نافذة حوار. */
  dialog: 60,
  /** نافذة فوق نافذة (اختيار كمية فوق سلة مفتوحة). */
  dialogNested: 70,
  /** حجاب الجولة الإرشادية والفتحة المضيئة فيه. */
  tourVeil: 85,
  tourSpot: 88,
  tourCallout: 95,
  /** رسائل مؤقّتة أسفل الشاشة. */
  toast: 90,
  /** حجاب انتقال الشاشات (دخول قسم من الرئيسية). */
  transition: 100,
  /** إشعارات النظام — فوق الانتقال كي لا يبتلعها. */
  notification: 120,
  /** نبضة إضاءة البيئة — تمرّ فوق كل شيء بلا اعتراض المؤشّر. */
  ambientLight: 130,
  /** شاشة الإقلاع تسبق التطبيق كلّه. */
  boot: 200,
  /** لوحة التنقيح — للتطوير فقط، فوق الجميع بلا منازع. */
  debug: 999,
} as const;

export type LayerName = keyof typeof LAYER;

/** للاستعمال في `style` مباشرة. */
export const z = (name: LayerName) => ({ zIndex: LAYER[name] });
